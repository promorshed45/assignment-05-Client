export type TService = {
    name: string;
    description: string;
    price: string;
    duration: string;
  };
  
 export type TServiceCardProps = {
    name: string;
    description: string;
    price: string;
    duration: string;
  };